[project]
name = "omnibus-dashboard"
version = "0.1.0"
description = "The Omnibus Dashboard"
requires-python = "~=3.11.0"
dependencies = ["omnibus", "numpy", "matplotlib>=3.10.0", "PySide6<=6.7.2", "pyqtgraph", "pyopengl"]

[tool.uv.sources]
omnibus = {workspace = true}
